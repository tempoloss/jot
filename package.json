{
  "name": "jot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Telegram task bot backed by GitHub Issues, on Cloudflare Workers",
  "scripts": {
    "test": "node --test test/*.test.js",
    "local": "node --env-file-if-exists=.dev.vars local.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --env-file-if-exists=.dev.vars local.js --check"
  }
}
